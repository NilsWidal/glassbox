{
  "mcpServers": {
    "glassbox": {
      "command": "npx",
      "args": ["-y", "@nilswidal/glassbox", "mcp"],
      "env": {
        "GLASSBOX_HOST": "claude-code",
        "GLASSBOX_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
