{
  "mcpServers": {
    "glassbox": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/plugin-dist/glassbox.mjs", "mcp"],
      "env": {
        "GLASSBOX_HOST": "claude-code",
        "GLASSBOX_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
