{
  "version": 1,
  "note": "Author-constructed labels: each truth follows from the fixture code, and `evidence` quotes the line that makes it true. Not an independent human-labeled set.",
  "repos": {
    "sample-repo": "../test/fixtures/sample-repo"
  },
  "items": [
    { "id": "db-sql-concat", "repo": "sample-repo", "paths": ["src/db.ts"], "type": "yesno", "question": "Does findUserByEmail build its SQL by concatenating the email argument into the query string?", "truth": "true", "evidence": "\"SELECT * FROM users WHERE email = '\" + email + \"'\"" },
    { "id": "db-parameterized", "repo": "sample-repo", "paths": ["src/db.ts"], "type": "yesno", "question": "Does this file use parameterized SQL queries (placeholders with separately bound values)?", "truth": "false", "evidence": "\"SELECT * FROM users WHERE email = '\" + email + \"'\"" },
    { "id": "db-network", "repo": "sample-repo", "paths": ["src/db.ts"], "type": "yesno", "question": "Does this file open a network connection to a database server?", "truth": "false", "evidence": "const rows = new Map<string, Row[]>();" },
    { "id": "db-insert-mutates", "repo": "sample-repo", "paths": ["src/db.ts"], "type": "yesno", "question": "Does insert() modify module-level state?", "truth": "true", "evidence": "rows.set(table, list);" },

    { "id": "mw-missing-header-passes", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "yesno", "question": "Does requireAuth call next() when the request has no Authorization header?", "truth": "true", "evidence": "if (!header) {" },
    { "id": "mw-admin-string", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "yesno", "question": "Does requireAdmin decide admin access by comparing the user id to the string 'admin'?", "truth": "true", "evidence": "if (req.userId === 'admin') next();" },
    { "id": "mw-null-session-rejects", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "yesno", "question": "Does requireAuth return false when verifySession returns no session?", "truth": "true", "evidence": "if (!session) return false;" },
    { "id": "mw-roles-db", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "yesno", "question": "Does this middleware look up user roles in a database?", "truth": "false", "evidence": "if (req.userId === 'admin') next();" },
    { "id": "mw-bearer-strip", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "yesno", "question": "Does requireAuth remove a 'Bearer ' prefix from the header before verifying the token?", "truth": "true", "evidence": "header.replace('Bearer ', '')" },

    { "id": "pw-salt", "repo": "sample-repo", "paths": ["src/auth/password.ts"], "type": "yesno", "question": "Does hashPassword add a salt before hashing?", "truth": "false", "evidence": "return createHash('md5').update(password).digest('hex');" },
    { "id": "pw-md5", "repo": "sample-repo", "paths": ["src/auth/password.ts"], "type": "yesno", "question": "Does hashPassword use MD5?", "truth": "true", "evidence": "return createHash('md5').update(password).digest('hex');" },
    { "id": "pw-constant-time", "repo": "sample-repo", "paths": ["src/auth/password.ts"], "type": "yesno", "question": "Is comparePassword a constant-time comparison?", "truth": "false", "evidence": "if (hashed[i] !== stored[i]) return false;" },
    { "id": "pw-slow-kdf", "repo": "sample-repo", "paths": ["src/auth/password.ts"], "type": "yesno", "question": "Does this file hash passwords with bcrypt, scrypt or argon2?", "truth": "false", "evidence": "return createHash('md5').update(password).digest('hex');" },

    { "id": "sess-ttl-nan", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "yesno", "question": "Can SESSION_TTL_MS be NaN when the SESSION_TTL environment variable is not set?", "truth": "true", "evidence": "const SESSION_TTL_MS = Number(process.env.SESSION_TTL) * 1000;" },
    { "id": "sess-secure-random", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "yesno", "question": "Are session tokens generated from a cryptographically secure random source?", "truth": "true", "evidence": "randomBytes(24).toString('hex')" },
    { "id": "sess-revoke-expired", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "yesno", "question": "Does verifySession revoke a session it finds expired?", "truth": "true", "evidence": "store.revoke(token);" },
    { "id": "sess-persisted", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "yesno", "question": "Are sessions persisted to disk or a database?", "truth": "false", "evidence": "private sessions = new Map<string, Session>();" },
    { "id": "sess-login-null", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "yesno", "question": "Does login return null when the password does not match?", "truth": "true", "evidence": "if (!user || !comparePassword(password, String(user.passwordHash))) return null;" },
    { "id": "sess-reads-clock", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "yesno", "question": "Does issueToken read the current time?", "truth": "true", "evidence": "expiresAt: Date.now() + SESSION_TTL_MS" },

    { "id": "routes-admin-sqli", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "yesno", "question": "Does handleAdminSearch put the search term directly into a SQL string?", "truth": "true", "evidence": "LIKE '%${term}%'" },
    { "id": "routes-create-auth", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "yesno", "question": "Does handleCreateInvoice run the invoice creation inside requireAuth?", "truth": "true", "evidence": "requireAuth(req, () => {" },
    { "id": "routes-login-401", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "yesno", "question": "Does handleLogin return status 401 when login fails?", "truth": "true", "evidence": "return session ? { status: 200, token: session.token } : { status: 401 };" },
    { "id": "routes-validate-customer", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "yesno", "question": "Does handleCreateInvoice validate customerId before using it?", "truth": "false", "evidence": "const invoice = invoices.createInvoice(body.customerId, []);" },
    { "id": "routes-cookie", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "yesno", "question": "Does handleLogin set a cookie on the response?", "truth": "false", "evidence": "return session ? { status: 200, token: session.token } : { status: 401 };" },

    { "id": "inv-discount-bounds", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "yesno", "question": "Does applyDiscount check that percent is between 0 and 100?", "truth": "false", "evidence": "return amount - (amount * percent) / 100;" },
    { "id": "inv-float-money", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "yesno", "question": "Does computeTotal do its money arithmetic on floating point numbers?", "truth": "true", "evidence": "sum + item.unitPrice * item.quantity" },
    { "id": "inv-cents", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "yesno", "question": "Does pay convert the total to cents before charging?", "truth": "true", "evidence": "Math.round(total * 100)" },
    { "id": "inv-create-writes-db", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "yesno", "question": "Does createInvoice write the invoice through the database layer?", "truth": "true", "evidence": "insert('invoices', { ...invoice });" },
    { "id": "inv-default-discount", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "yesno", "question": "Does createInvoice give new invoices a non-zero discount?", "truth": "false", "evidence": "discountPercent: 0" },

    { "id": "stripe-logs-card", "repo": "sample-repo", "paths": ["src/billing/stripeClient.ts"], "type": "yesno", "question": "Does chargeCard log the full card number?", "truth": "true", "evidence": "charging card ${cardNumber}" },
    { "id": "stripe-positive", "repo": "sample-repo", "paths": ["src/billing/stripeClient.ts"], "type": "yesno", "question": "Does chargeCard reject amounts that are zero or negative?", "truth": "true", "evidence": "if (amountCents <= 0) throw new Error('amount must be positive');" },
    { "id": "stripe-network", "repo": "sample-repo", "paths": ["src/billing/stripeClient.ts"], "type": "yesno", "question": "Does chargeCard call a payment API over the network?", "truth": "false", "evidence": "status: 'succeeded' };" },
    { "id": "stripe-refund-real", "repo": "sample-repo", "paths": ["src/billing/stripeClient.ts"], "type": "yesno", "question": "Does refund reverse the charge with a payment provider?", "truth": "false", "evidence": "return chargeId.startsWith('ch_');" },

    { "id": "retry-idempotency", "repo": "sample-repo", "paths": ["src/billing/retry.ts"], "type": "yesno", "question": "Does retryCharge send an idempotency key with each charge attempt?", "truth": "false", "evidence": "return await chargeCard(cardNumber, amountCents);" },
    { "id": "retry-backoff", "repo": "sample-repo", "paths": ["src/billing/retry.ts"], "type": "yesno", "question": "Does retryCharge wait longer after each failed attempt (exponential backoff)?", "truth": "true", "evidence": "await sleep(2 ** i * 100);" },
    { "id": "retry-rethrow", "repo": "sample-repo", "paths": ["src/billing/retry.ts"], "type": "yesno", "question": "Does retryCharge throw the last error after every attempt fails?", "truth": "true", "evidence": "throw lastError;" },

    { "id": "ui-raw-html", "repo": "sample-repo", "paths": ["src/ui/LoginForm.tsx"], "type": "yesno", "question": "Does LoginForm render the message prop as raw HTML?", "truth": "true", "evidence": "dangerouslySetInnerHTML={{ __html: message ?? '' }}" },
    { "id": "ui-password-type", "repo": "sample-repo", "paths": ["src/ui/LoginForm.tsx"], "type": "yesno", "question": "Is the password input declared with type=\"password\"?", "truth": "true", "evidence": "<input name=\"password\" type=\"password\" />" },
    { "id": "ui-submits-inputs", "repo": "sample-repo", "paths": ["src/ui/LoginForm.tsx"], "type": "yesno", "question": "Does the form's onSubmit pass the values typed into the email and password inputs to submit?", "truth": "false", "evidence": "<form onSubmit={() => submit('', '')}>" },
    { "id": "fmt-usd-default", "repo": "sample-repo", "paths": ["src/ui/format.ts"], "type": "yesno", "question": "Does formatCurrency default to US dollars?", "truth": "true", "evidence": "currency = 'USD'" },
    { "id": "fmt-date-time", "repo": "sample-repo", "paths": ["src/ui/format.ts"], "type": "yesno", "question": "Does formatDate include the time of day in its output?", "truth": "false", "evidence": "new Date(ts).toISOString().slice(0, 10)" },
    { "id": "table-own-total", "repo": "sample-repo", "paths": ["src/ui/InvoiceTable.tsx"], "type": "yesno", "question": "Does InvoiceTable compute invoice totals itself instead of calling a function passed in its props?", "truth": "false", "evidence": "<td>{formatCurrency(total(inv))}</td>" },

    { "id": "py-shell-true", "repo": "sample-repo", "paths": ["worker/billing_sync.py"], "type": "yesno", "question": "Does fetch_invoices run a shell command built from caller-controlled input?", "truth": "true", "evidence": "\"billing-cli export --customer \" + customer_id, shell=True, capture_output=True, text=True" },
    { "id": "py-sync-retries", "repo": "sample-repo", "paths": ["worker/billing_sync.py"], "type": "yesno", "question": "Does sync_invoices retry fetch_invoices when it fails?", "truth": "true", "evidence": "rows = with_retry(lambda: fetch_invoices(customer_id))" },
    { "id": "py-escape-customer", "repo": "sample-repo", "paths": ["worker/billing_sync.py"], "type": "yesno", "question": "Does fetch_invoices escape or quote customer_id before putting it in the command?", "truth": "false", "evidence": "\"billing-cli export --customer \" + customer_id, shell=True, capture_output=True, text=True" },
    { "id": "py-decimal", "repo": "sample-repo", "paths": ["worker/utils.py"], "type": "yesno", "question": "Does parse_amount use Decimal for money?", "truth": "false", "evidence": "return float(text.replace(\"$\", \"\").replace(\",\", \"\"))" },
    { "id": "py-retry-sleeps", "repo": "sample-repo", "paths": ["worker/utils.py"], "type": "yesno", "question": "Does with_retry sleep between attempts?", "truth": "true", "evidence": "time.sleep(2 ** i * 0.1)" },
    { "id": "py-narrow-except", "repo": "sample-repo", "paths": ["worker/utils.py"], "type": "yesno", "question": "Does with_retry catch only specific exception types?", "truth": "false", "evidence": "except Exception as err:  # noqa: BLE001" },
    { "id": "py-pickle-load", "repo": "sample-repo", "paths": ["worker/auth_audit.py"], "type": "yesno", "question": "Does AuditLog.load deserialize the file with pickle?", "truth": "true", "evidence": "return pickle.load(fh)" },
    { "id": "py-record-flushes", "repo": "sample-repo", "paths": ["worker/auth_audit.py"], "type": "yesno", "question": "Does AuditLog.record write the log to disk on every call?", "truth": "true", "evidence": "self.flush()" },
    { "id": "py-audit-json", "repo": "sample-repo", "paths": ["worker/auth_audit.py"], "type": "yesno", "question": "Does the audit log store its events as JSON?", "truth": "false", "evidence": "pickle.dump(self.events, fh)" },
    { "id": "py-tmp-path", "repo": "sample-repo", "paths": ["worker/main.py"], "type": "yesno", "question": "Does run write the audit log to a fixed path under /tmp?", "truth": "true", "evidence": "log = AuditLog(\"/tmp/audit.pkl\")" },
    { "id": "py-argv", "repo": "sample-repo", "paths": ["worker/main.py"], "type": "yesno", "question": "Does the script read the customer id from command line arguments?", "truth": "false", "evidence": "run(\"c1\")" },

    { "id": "ch-token-issuer", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "choice", "question": "Which function creates new session tokens?", "options": { "login": "login()", "issueToken": "issueToken()", "verifySession": "verifySession()", "revoke": "SessionStore.revoke()" }, "truth": "issueToken", "evidence": "const session = { token: randomBytes(24).toString('hex'), userId, expiresAt: Date.now() + SESSION_TTL_MS };" },
    { "id": "ch-hash-algo", "repo": "sample-repo", "paths": ["src/auth/password.ts"], "type": "choice", "question": "Which hash algorithm does hashPassword use?", "options": { "md5": "MD5", "sha256": "SHA-256", "bcrypt": "bcrypt", "argon2": "Argon2" }, "truth": "md5", "evidence": "return createHash('md5').update(password).digest('hex');" },
    { "id": "ch-discount-method", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "choice", "question": "Which method applies the discount to the subtotal?", "options": { "computeTotal": "computeTotal()", "applyDiscount": "applyDiscount()", "createInvoice": "createInvoice()", "pay": "pay()" }, "truth": "applyDiscount", "evidence": "return this.applyDiscount(subtotal, invoice.discountPercent);" },
    { "id": "ch-admin-guard", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "choice", "question": "Which guard wraps the query in handleAdminSearch?", "options": { "requireAuth": "requireAuth", "requireAdmin": "requireAdmin", "none": "no guard" }, "truth": "requireAdmin", "evidence": "requireAdmin(req, () => {" },
    { "id": "ch-export-module", "repo": "sample-repo", "paths": ["worker/billing_sync.py"], "type": "choice", "question": "Which Python module does fetch_invoices use to run the export?", "options": { "subprocess": "subprocess", "os": "os.system", "requests": "requests (HTTP)", "socket": "socket" }, "truth": "subprocess", "evidence": "out = subprocess.run(" },
    { "id": "ch-audit-format", "repo": "sample-repo", "paths": ["worker/auth_audit.py"], "type": "choice", "question": "Which serialization format does AuditLog use on disk?", "options": { "json": "JSON", "pickle": "pickle", "csv": "CSV", "yaml": "YAML" }, "truth": "pickle", "evidence": "pickle.dump(self.events, fh)" },
    { "id": "ch-credential-source", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "choice", "question": "Where does requireAuth read the credentials from?", "options": { "header": "the Authorization header", "cookie": "a cookie", "query": "the query string", "body": "the request body" }, "truth": "header", "evidence": "const header = req.headers['authorization'];" },
    { "id": "ch-charge-call", "repo": "sample-repo", "paths": ["src/billing/retry.ts", "src/billing/stripeClient.ts"], "type": "choice", "question": "What does retryCharge call to charge the card?", "options": { "chargeCard": "chargeCard()", "refund": "refund()", "fetch": "fetch() to an HTTP API", "sdk": "a payment SDK client" }, "truth": "chargeCard", "evidence": "return await chargeCard(cardNumber, amountCents);" },
    { "id": "ch-currency-api", "repo": "sample-repo", "paths": ["src/ui/format.ts"], "type": "choice", "question": "How does formatCurrency format the amount?", "options": { "intl": "Intl.NumberFormat", "toFixed": "Number.prototype.toFixed", "manual": "manual string building", "library": "a third-party library" }, "truth": "intl", "evidence": "new Intl.NumberFormat('en-US', { style: 'currency', currency }).format(amount)" },
    { "id": "ch-login-call", "repo": "sample-repo", "paths": ["src/ui/LoginForm.tsx"], "type": "choice", "question": "Which function does LoginForm call to authenticate the user?", "options": { "login": "login()", "verifySession": "verifySession()", "issueToken": "issueToken()", "fetch": "fetch()" }, "truth": "login", "evidence": "const session = await login(email, password);" },
    { "id": "ch-row-store", "repo": "sample-repo", "paths": ["src/db.ts"], "type": "choice", "question": "What holds the table rows in db.ts?", "options": { "map": "an in-memory Map", "array": "a plain array", "sqlite": "a SQLite database", "file": "a file on disk" }, "truth": "map", "evidence": "const rows = new Map<string, Row[]>();" },
    { "id": "ch-expired-session", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "choice", "question": "What does verifySession do with an expired session?", "options": { "revoke_null": "revokes it and returns null", "throw": "throws an error", "refresh": "extends its expiry", "return": "returns it unchanged" }, "truth": "revoke_null", "evidence": "store.revoke(token);" },

    { "id": "sc-retry-attempts", "repo": "sample-repo", "paths": ["src/billing/retry.ts"], "type": "score", "question": "How many charge attempts does retryCharge make by default?", "levels": ["1", "2", "3", "4 or more"], "truth": "2", "evidence": "attempts = 3" },
    { "id": "sc-py-retry-attempts", "repo": "sample-repo", "paths": ["worker/utils.py"], "type": "score", "question": "How many attempts does with_retry make by default?", "levels": ["1", "2", "3", "4 or more"], "truth": "2", "evidence": "def with_retry(fn, attempts=3):" },
    { "id": "sc-token-bytes", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "score", "question": "How many random bytes go into a session token?", "levels": ["8 or fewer", "16", "24", "32 or more"], "truth": "2", "evidence": "randomBytes(24)" },
    { "id": "sc-route-handlers", "repo": "sample-repo", "paths": ["src/api/routes.ts"], "type": "score", "question": "How many exported functions does this file define?", "levels": ["0", "1", "2", "3", "4 or more"], "truth": "3", "evidence": "export function handleAdminSearch(req: Request, term: string) {" },
    { "id": "sc-store-methods", "repo": "sample-repo", "paths": ["src/auth/session.ts"], "type": "score", "question": "How many methods does the SessionStore class define?", "levels": ["0", "1", "2", "3", "4 or more"], "truth": "3", "evidence": "revoke(token: string): void {" },
    { "id": "sc-invoice-methods", "repo": "sample-repo", "paths": ["src/billing/invoice.ts"], "type": "score", "question": "How many methods does the InvoiceService class define?", "levels": ["1", "2", "3", "4", "5 or more"], "truth": "3", "evidence": "async pay(invoice: Invoice, cardNumber: string): Promise<boolean> {" },
    { "id": "sc-next-calls", "repo": "sample-repo", "paths": ["src/auth/middleware.ts"], "type": "score", "question": "How many separate next() call sites are inside requireAuth?", "levels": ["0", "1", "2", "3 or more"], "truth": "2", "evidence": "req.userId = session.userId;" },
    { "id": "sc-password-exports", "repo": "sample-repo", "paths": ["src/auth/password.ts"], "type": "score", "question": "How many exported functions does this file define?", "levels": ["0", "1", "2", "3 or more"], "truth": "2", "evidence": "export function comparePassword(password: string, stored: string): boolean {" },
    { "id": "sc-audit-methods", "repo": "sample-repo", "paths": ["worker/auth_audit.py"], "type": "score", "question": "How many methods does the AuditLog class define, counting __init__?", "levels": ["1", "2", "3", "4", "5 or more"], "truth": "3", "evidence": "def load(path):" },
    { "id": "sc-stripe-exports", "repo": "sample-repo", "paths": ["src/billing/stripeClient.ts"], "type": "score", "question": "How many exported functions does this file define?", "levels": ["0", "1", "2", "3 or more"], "truth": "2", "evidence": "export async function refund(chargeId: string): Promise<boolean> {" }
  ]
}
